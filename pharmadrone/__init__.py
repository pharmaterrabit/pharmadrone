"""PharmaDrone — evidence-disciplined pharma BD case-study generator."""
__version__ = "1.0.0"
CHECKPOINT = "Checkpoint 6C.1 active — Scheduled incremental refresh"

# Expose the existing submodule for frozen regression patch targets.
from . import llm as llm
